export const mayaTokens = {
  colors: {
    primary: 'hsl(var(--primary))',
    primaryLight: 'hsl(var(--primary) / 0.1)',
    sidebar: 'hsl(var(--muted))',
    sidebarBorder: 'hsl(var(--border))',
    conversationActive: 'hsl(var(--primary) / 0.1)',
    conversationHover: 'hsl(var(--muted) / 0.5)',
  },
  spacing: {
    sidebarWidth: '288px', // w-72
    sidebarCollapsed: '64px', // w-16
  },
  transitions: {
    sidebar: 'all 300ms cubic-bezier(0.4, 0, 0.2, 1)',
  }
};
